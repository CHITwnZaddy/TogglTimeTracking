# Environment / secrets — never commit these
.env
.env.*

# Streamlit secrets — never commit the real one
.streamlit/secrets.toml

# Invoice counter (local state, not needed in repo)
invoice_counter.json

# Client email map (contains PII — never commit)
client_emails.json

# Mac cruft
.DS_Store

# Node
node_modules/
package-lock.json
package.json
